android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g
# No org.gradle.java.home here on purpose: the upstream project pinned a macOS
# Homebrew JDK path, which is not portable. Gradle uses JAVA_HOME / the JDK on
# PATH instead, so this builds on whatever host has a supported JDK.
